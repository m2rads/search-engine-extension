/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './*.html', // Adjust the path to where your HTML files are
    './*.js',     // Adjust the path to where your JavaScript files are
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
